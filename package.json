{
  "name": "blog",
  "version": "1.0.0",
  "description": "Personal blog built on [Hexo](https://hexo.io) framework.",
  "main": "index.js",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "start": "eleventy --serve",
    "deploy": "gh-pages -d _site",
    "build:deploy": "npm run build && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tormozz48/blog.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0",
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
